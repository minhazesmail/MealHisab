import Link from 'next/link'

export default function JoinPage() {
  return <main className="min-h-screen bg-canvas px-4 py-10 text-main"><div className="mx-auto max-w-xl"><Link href="/account-type" className="text-sm font-semibold text-muted hover:text-main">← Back</Link><div className="card mt-6 space-y-5"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-brand-green">Join your mess</p><h1 className="mt-2 text-3xl font-black">Enter your Flat Code</h1><p className="mt-2 text-sm leading-6 text-muted">Your manager gave you a single-use invite code. Enter it first, then verify your phone or email with OTP. Joining is free.</p></div><form action={(formData)=>{ 'use server'; const code=String(formData.get('code')||'').trim().toUpperCase(); const target=code?`/login?role=member&invite=${encodeURIComponent(code)}`:'/join'; require('next/navigation').redirect(target) }} className="space-y-4"><input name="code" className="input text-center uppercase tracking-[0.3em]" placeholder="AB7KQ9Z2" minLength={6} maxLength={16} required autoComplete="off"/><button className="btn-primary w-full">Continue to OTP sign up / login</button></form><p className="text-center text-xs text-muted">No payment required for members.</p></div></div></main>
}
